# Copy this file to .env.local and fill in your values.
# .env.local is gitignored and must NEVER be committed.
#
# VITE_ prefix makes variables available in the browser bundle.
# These values will be visible to anyone who views your site's source —
# restrict each key to your domain in the provider's dashboard.

# Google Maps JavaScript API key
# Restrict to: https://weather.robbutler.com (HTTP referrers) in Google Cloud Console
VITE_GOOGLE_MAPS_KEY=
